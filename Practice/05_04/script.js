/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */


document.querySelector(".page-header__heading").style.color = "#154734";
document.querySelectorAll(".feature").forEach(item => item.style.backgroundColor = "#154734");
document.querySelectorAll(".feature").forEach(item => item.style.padding = "10px");
document.querySelectorAll(".feature").forEach(item => item.style.color = "white");